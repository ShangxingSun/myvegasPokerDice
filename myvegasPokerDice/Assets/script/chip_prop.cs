using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class chip_prop : MonoBehaviour
{
    // Start is called before the first frame update
    public double bet_value;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
